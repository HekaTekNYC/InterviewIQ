I have imported all the files from frontend-backend-01-starting-setup section except the following:

    -- App.js
    -- Index.css
    -- Index.js

      console.log('Error in Users component: ', error);

  const errorHandler = () => {
    setError(null);
  };